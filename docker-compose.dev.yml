version: '3.8'

services:
  cojakeons-plus-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: cojakeons-plus-dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true
    networks:
      - cojakeons-network
    stdin_open: true
    tty: true

networks:
  cojakeons-network:
    driver: bridge
